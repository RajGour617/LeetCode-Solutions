# Time Complexity: O(n)
# Space Complexity: O(1)

class Solution {
    public int reverseDegree(String s) {
        int res = 0;

        for (int i = 0; i < s.length(); i++) {
            res += (i + 1) * ('z' - s.charAt(i) + 1);
        }

        return res;
    }
}
